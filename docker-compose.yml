services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
      - TIMEZONE=${TIMEZONE:-Europe/Paris}
      - DATABASE_PATH=/data/lifelog.db
    volumes:
      - lifelog_data:/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  lifelog_data: